{
  "name": "moonlight",
  "private": true,
  "version": "0.1.0",
  "description": "Moonlight — a personal creative hub: calendar, tasks, projects, progress, and GitHub, native on iOS and Windows.",
  "workspaces": [
    "packages/core",
    "packages/mobile",
    "packages/desktop"
  ],
  "scripts": {
    "build:core": "npm run build --workspace packages/core",
    "test": "npm run test --workspace packages/core",
    "lint": "eslint packages/core/src packages/desktop/src packages/mobile/App.tsx packages/mobile/src --ext .ts,.tsx",
    "typecheck": "npm run typecheck --workspace packages/core && npm run typecheck --workspace packages/desktop && npm run typecheck --workspace packages/mobile",
    "verify": "npm run lint && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^4.6.2",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
